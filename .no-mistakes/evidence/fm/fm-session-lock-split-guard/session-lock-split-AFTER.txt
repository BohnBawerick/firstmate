===================================================================
  AFTER the change (target commit faf35c0)
  session A (harness pid 39975) holds this home. Everything below
  is what a SECOND live session in the same home sees.
===================================================================

--- session B: what the lock path tells it (part 2) ---------------
$ bin/fm-lock.sh
error: NOT THIS SESSION - another live firstmate session holds the lock (harness pid 39975); operate read-only until resolved
[exit 1]

$ bin/fm-lock.sh status
lock: held by ANOTHER live session (harness pid 39975)
[exit 0]

--- session B: the session-start digest HELM line (part 2) --------
$ bin/fm-session-start.sh   (LOCK section)
LOCK
--------------------------------------------------------------------------------
error: NOT THIS SESSION - another live firstmate session holds the lock (harness pid 39975); operate read-only until resolved
HELM: ANOTHER session holds the fleet lock - this session is READ-ONLY.
●━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
●  READ-ONLY SESSION - FLEET LOCK OWNERSHIP WAS NOT VERIFIED
●  error: NOT THIS SESSION - another live firstmate session holds the lock (harness pid 39975); operate read-only until resolved
●  Skipping every mutating step: PR-check migration, stale Herdr child cleanup,
●  secondmate convergence, secondmate liveness, pending remote handoff retry,
●  X-mode artifacts, fleet sync, and wake-queue drain. Detect-only bootstrap
●  diagnostics and the rest of this read-only-safe digest still ran below.
●  Operate read-only until this resolves - do not spawn, steer, merge, or
●  otherwise mutate fleet state from this session.
●━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


--- session B: mutating fleet paths (part 1) ----------------------
$ bin/fm-spawn.sh
error: refusing to spawn a worker - this session does not hold the fleet lock for /tmp/fmdemo/run.XlIAhp/home/state.
Another live firstmate session (harness pid 39975) holds it, and only that session
may change fleet state. Operate read-only from here, or end that session first.
[exit 1]
$ bin/fm-send.sh
error: refusing to steer a worker - this session does not hold the fleet lock for /tmp/fmdemo/run.XlIAhp/home/state.
Another live firstmate session (harness pid 39975) holds it, and only that session
may change fleet state. Operate read-only from here, or end that session first.
[exit 1]
$ bin/fm-merge-local.sh
error: refusing to merge work into local main - this session does not hold the fleet lock for /tmp/fmdemo/run.XlIAhp/home/state.
Another live firstmate session (harness pid 39975) holds it, and only that session
may change fleet state. Operate read-only from here, or end that session first.
[exit 1]
$ bin/fm-pr-merge.sh
error: refusing to merge a pull request - this session does not hold the fleet lock for /tmp/fmdemo/run.XlIAhp/home/state.
Another live firstmate session (harness pid 39975) holds it, and only that session
may change fleet state. Operate read-only from here, or end that session first.
[exit 1]
$ bin/fm-teardown.sh
error: refusing to tear a task down - this session does not hold the fleet lock for /tmp/fmdemo/run.XlIAhp/home/state.
Another live firstmate session (harness pid 39975) holds it, and only that session
may change fleet state. Operate read-only from here, or end that session first.
[exit 1]
$ bin/fm-promote.sh
error: refusing to promote a scout task - this session does not hold the fleet lock for /tmp/fmdemo/run.XlIAhp/home/state.
Another live firstmate session (harness pid 39975) holds it, and only that session
may change fleet state. Operate read-only from here, or end that session first.
[exit 1]
$ bin/fm-control.sh
error: refusing to drive a worker lifecycle action - this session does not hold the fleet lock for /tmp/fmdemo/run.XlIAhp/home/state.
Another live firstmate session (harness pid 39975) holds it, and only that session
may change fleet state. Operate read-only from here, or end that session first.
[exit 1]
$ bin/fm-wake-drain.sh
error: refusing to present or acknowledge this home's wake queue - this session does not hold the fleet lock for /tmp/fmdemo/run.XlIAhp/home/state.
Another live firstmate session (harness pid 39975) holds it, and only that session
may change fleet state. Operate read-only from here, or end that session first.
[exit 1]

--- session B: ending two turns (part 3) --------------------------
$ turn 1 ends
●━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
●  SUPERVISION IS OFF, AND THIS SESSION CANNOT TURN IT ON
●  Another live firstmate session (harness pid 39975) holds this home fleet lock,
●  so supervision is that session job and nothing here can arm or repair it.
●  This session is READ-ONLY: do not spawn, steer, merge, or otherwise change
●  fleet state from it. End the other session, or continue the work there.
●  Reported once - later turns will not block on this again.
●━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[exit 2]
  state/.turnend-claude-blocks: (absent)

$ turn 2 ends
[exit 0]
  state/.turnend-claude-blocks: (absent)

$ turn 3 ends
[exit 0]
  state/.turnend-claude-blocks: (absent)

$ turn 4 ends
[exit 0]
  state/.turnend-claude-blocks: (absent)

$ turn 5 ends
[exit 0]
  state/.turnend-claude-blocks: (absent)

$ turn 6 ends
[exit 0]
  state/.turnend-claude-blocks: (absent)


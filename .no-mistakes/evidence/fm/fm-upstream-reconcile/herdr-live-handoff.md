The selected Herdr live scenario passed using agy 1.2.2 in a guarded named session.
The worker survived deliberately unconfirmed startup, accepted durable steering during injected liveness-read failures, and completed its shell tool after recovery refused.
Guarded teardown passed and the default-session tripwire remained unchanged.
See `herdr-live-result.json`, `herdr-live.log`, and `live-herdr.sh` for the exact checks and limitations.

## Live coverage not exercised

- Native Pi supervision continuity through AFK and return with a real captain.
- Parent and child teardown retry after real endpoint failure.
- Remote home update across SSH, because no disposable remote home exists.
- No-mistakes cleanup on a disposable pipeline, because no disposable target exists.

The user accepted fixture and targeted regression evidence for these four gaps.
The outer executor must include this section in the PR description.

{
  "verdict": "go within the accepted partial-validation scope",
  "live_command": "bash /home/bohn/.no-mistakes/evidence/01M2FSAP36PE8TM5G7KKWCZCFM/herdr-recheck/live-herdr.sh",
  "live_exit_code": 0,
  "regression_command": "TMPDIR=\"$PWD/.test-phase-herdr/regression-tmp\" bash tests/fm-agy-harness.test.sh",
  "regression_exit_code": 0,
  "observations": [
    "Production spawn returned startup unconfirmed with the readiness polling budget set to zero, retaining the endpoint and ownership records.",
    "A real agy worker ran a 90-second shell tool.",
    "Fault-injected missing registration and unavailable process-info reads produced unreadable liveness.",
    "Production fm-send persisted steering; production fm-control relaunch refused, preserving byte-identical metadata and the live shell process.",
    "The shell tool completed and the worker handled inbox/001.msg, writing 80235.",
    "Guarded teardown exited zero; the named lab session was absent afterward and the default-session snapshot matched the baseline."
  ],
  "limits": [
    "The live worker was agy, not Rovo. Only the liveness read failures were injected; the worker, backend, shell tool, steering and recovery commands were real.",
    "The helper measures default-session name, default flag, running state and socket path.",
    "Earlier targeted and live evidence remains in the parent evidence directory; this follow-up re-drove the required Herdr scenario and the changed agy regression."
  ]
}
